{
  "name": "dropout-prediction-system",
  "version": "1.0.0",
  "description": "Student Dropout Prediction and Counselling System",
  "scripts": {
    "start": "./start.sh",
    "backend": "source .venv-1/bin/activate && cd backend && uvicorn app:app --reload --host 0.0.0.0 --port 8000",
    "frontend": "cd frontend-react && npm start",
    "install-all": "cd frontend-react && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
